var api = {};

api.updateScore = function(user){
  return new Promise((resolve, reject)=>{
    // ebay.getScore.then((score)=>{
    //   //
    // });
    //chain off yelp  and uber and then 
    //resolve(finalScore);
  });
};

api.getScore = function(user){
  return new Promise((resolve, reject)=>{
    //db.getMarketScore.then
    //resolve(gottenScore);
  });
};

return api;