var api = {};

api.updateScore = function(user){
  return new Promise((resolve, reject)=>{
    // market.getScore.then((score)=>{
    //   //
    // });
    //chain off market and social and then 
    //resolve(finalScore);
  });
};

api.getScore = function(user){
  return new Promise((resolve, reject)=>{
    //db.getScore.then
    //resolve(gottenScore);
  });
};

return api;