var api = {};

api.updateScore = function(user){
  return new Promise((resolve, reject)=>{
    // private function getFacebookPosts.then((posts)=>{
    //   haven.calculateSentiment(posts).then((sentiment)=>{
          //db.set(sentiment);
    // })
    // });
  });
};

api.getScore = function(user){
  return new Promise((resolve, reject)=>{
    //db.getScore.then
    //resolve(gottenScore);
  });
};

//private function
function getFacebookPosts(username){
  return new Promise((resolve, reject)=>{
    //get the facebook posts and
    //resolve(posts);
  });
}


module.exports =  api;